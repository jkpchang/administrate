<%#
# Flash Partial

This partial renders flash messages on every page.

## Relevant Helpers:

- `flash`:
  Returns a hash,
  where the keys are the type of flash (alert, error, notice, etc)
  and the values are the message to be displayed.
%>

<% if flash[:notice].present? %>
  <div class="alert alert-info"><%= flash[:notice].html_safe unless flash[:notice].blank? %></div>
<% end %>

<% if flash[:alert].present? %>
  <div class="alert alert-warning"><%= flash[:alert].html_safe unless flash[:alert].blank? %></div>
<% end %>

<% if flash[:error].present? %>
  <div class="alert alert-danger"><%= flash[:error].html_safe unless flash[:error].blank? %></div>
<% end %>
